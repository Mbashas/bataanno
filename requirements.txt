# Multi-Country Water Services Performance Dashboard
# Python dependencies

# Core framework
streamlit>=1.28.0
# starlette 1.4.0 made `thread_minimum_size` a required kwarg on
# GZipResponder.__init__. Streamlit's MediaAwareGZipMiddleware subclasses it
# without passing that kwarg, so every /_stcore/* request 500s and the app
# fails its health checks. Remove this pin once Streamlit ships a fix.
starlette<1.4
streamlit-authenticator>=0.3.1

# LLM integration (New Addition!)
google-generativeai>=0.3.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# Visualization
plotly>=5.17.0

# Date/time handling
python-dateutil>=2.8.0

# Configuration parsing
PyYAML>=6.0  # Required for users.yaml authentication config

# Optional but recommended
openpyxl>=3.1.0  # For Excel export
xlsxwriter>=3.1.0  # For Excel formatting
fpdf2>=2.7.5  # PDF report generation

